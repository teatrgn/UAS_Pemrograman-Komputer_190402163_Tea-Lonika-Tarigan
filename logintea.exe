# Nama : Tea Lonika Tarigan
# Nim : 190402163
# Ujian Pemograman Komputer

# Membuat Login Sederhana
from tkinter import *

root = Tk()

username = Label(root, text='Username :')
password = Label(root, text='Password :')
userbox = Entry(root)
passbox = Entry(root)

signin = Button(root, text='Sign In')
signup = Button(root, text='Sign Up')

username.grid(row=0, column=0)
password.grid(row=1, column=0)
userbox.grid(row=0, column=1)
passbox.grid(row=1, column=1)


signin.grid(row=2, column=2)
signup.grid(row=2, column=3)

root.mainloop()

# data data indetitas diri
nama = "Tea Lonika Tarigan"
umur = 20
asal = "medan"
gender = "Wanita"

print("identitas diri : " + nama + " " + str(umur) + " " + asal + " " + gender)

# loop
i = 1
while i < 6:
  print(i)
  i += 1
while i > 0:
  print(i)
  i -= 1

# List
a = {"1,2,3,4"}
print(a)
print(len(a))
print(type(a))